load("@rules_go//go:def.bzl", "go_binary", "go_library")

go_library(
    name = "portal_lib",
    srcs = [
        "handler.go",
        "main.go",
        "routes.go",
    ],
    importpath = "app/cmd/portal",
    visibility = ["//visibility:private"],
    deps = [
        "//internal/models",
        "//registry",
        "//service",
    ],
)

go_binary(
    name = "portal",
    embed = [":portal_lib"],
    visibility = ["//visibility:public"],
)
